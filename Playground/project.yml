include:
  - project-base.yml

schemes:
  Playground:
    test:
      testPlans:
        # Generated by `make playground` from Playground.xctestplan, without
        # the LCP tests.
        - path: Playground+nolcp.xctestplan
          defaultPlan: true
